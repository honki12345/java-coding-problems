package chapter05;

import org.junit.jupiter.api.Test;

public class Prob123 {
    @Test
    void test() {
       /*가장 쉽게 알고리즘을 이해하는 방법은
       * 의사코드와 예제다
       * 너비 우선 탐색의 의사코드
       * 1. 큐Q를 생성한다
       * 2. v에 방문했다고 표시하고 Q에 v를 넣는다
       * 3. Q가 빌때까지
       * Q의 헤더 h를 삭제한다
       * h의 모든(방문하지 않은) 이웃에 방문했다고 표시하고 큐에 넣는다*/
    }
}
